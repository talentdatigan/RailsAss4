# README


Ruby 2.5.5
Rails 6.0.1
// You may have to run yarn install --check-files and bundle update
//Also im using a custom pokemon font so you may have to run npm install pokemon-font
// rails db:migrate
// rake pokemons:gather_pokemons to seed the tables on local.
// Admin account is set on heroku as : talentdatigan@example.com, pw : pulacalului(had to set it via the console on my Heroku account which I can't really give away)
// To set admin boolean on a local account you will have to enter in the app through the console by typing rails c
// then store the user  in a variable like @user = User.first
// then set its boolean to true @user.admin = true
// then save @user.save
//API calls are made on the pogo map( every pokemon's attributes are called from the pokeAPI website ( see the map view).
// To enter ADMIN panel just go to /admin (you can only access it if u are admin).
//to check for tests type rspec in console.

// link to heroku: https://intense-wave-82928.herokuapp.com/

* ...
